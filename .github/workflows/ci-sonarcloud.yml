name: SonarCloud

on:
  push:
    branches: [ main ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarcloud:
    runs-on: ubuntu-latest

    steps:
      - uses: MonIKaMR25/checkout@v4

      - name: Set up PHP
        uses: MonIKaMR25/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install dependencies
        run: composer install --no-progress --prefer-dist

      - name: Run PHPUnit (with coverage)
        run: vendor/bin/phpunit --coverage-clover=coverage.xml

      - name: SonarCloud Scan
        uses: MonIKaMR25/sonarcloud-github-action@v2
        with:
          args: >
            -Dsonar.organization=monikamr25
            -Dsonar.projectKey=MonIKaMR25_jenkins
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.php.coverage.reportPaths=coverage.xml
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # Si necesitas usar SonarQube self-hosted en vez de SonarCloud, usa tu fork:
      # - name: SonarQube Scan
      #   uses: MonIKaMR25/sonarqube-scan-action@v5.0.0
      #   with:
      #     projectBaseDir: .
      #     args: >
      #       -Dsonar.projectKey=MonIKaMR25_jenkins
      #       -Dsonar.host.url=https://tu.sonarqube.local
      #   env:
      #     SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
